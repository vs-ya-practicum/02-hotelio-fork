version: v2

replicaCount: 1

image:
  name: booking-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080
  name: booking-service
  create: false

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

livenessProbe:

readinessProbe:

env:
  - name: ENABLE_FEATURE_X
    value: 'true'
  - name: SERVICE_VERSION
    value: 'v2'
