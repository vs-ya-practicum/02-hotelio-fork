query ValidatePromoCode($code: String!, $hotelId: ID) {
    validatePromoCode(code: $code, hotelId: $hotelId) {
        isValid
        originalDiscount
        finalDiscount
        description
        expiresAt
        applicableHotels
    }
}
