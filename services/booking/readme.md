## Notes

> [!INFO]
> NPM scripts `service:compose:up` and `service:compose:down` must be run in the environment where Docker is installed.

contract: hotelio-fork\services\booking\contract\booking.oas.json
delivery: hotelio-fork\services\booking\.delivery\configuration